/*
  # Skip Policy Creation
  
  The policies have already been created in previous migrations:
  - Users can view bots
  - Users can create bots
  - Users can update bots

  This migration is now a no-op to avoid conflicts.
*/

-- No changes needed - already applied in previous migrations
SELECT 1;